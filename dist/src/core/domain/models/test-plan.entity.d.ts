export declare class TestPlan {
    id: string;
    name: string;
    sha: string;
    url: string;
    createdAt: Date;
    updatedAt: Date;
    constructor(id: string, name: string, sha: string, url: string, createdAt: Date, updatedAt: Date);
}
